interface:
  display_name: "Update Codex Protocol"
  short_description: "Regenerate and optionally tag protocol releases."
  default_prompt: "Use $update-codex-protocol to regenerate codex-sdk-go protocol files from the latest stable upstream Codex Rust release, run tests, commit validated changes, and create or push an SDK release tag when requested."
