interface:
  display_name: "Update Codex Protocol"
  short_description: "Update, review, and hand off an SDK release."
  default_prompt: "Use $update-codex-protocol to update codex-sdk-go from the latest stable Codex Rust release and carry the reviewed change through its protected GitHub release handoff."
